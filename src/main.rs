use libp2p::core::upgrade;
use libp2p::identity;
use libp2p::multiaddr;
use libp2p::noise;
use libp2p::quic;
use libp2p::swarm;
use libp2p::tcp;
use libp2p::yamux;

#[tokio::main]
async fn main() {
    println!("Hello, world!");
}