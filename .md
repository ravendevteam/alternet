```bash
netstat -tuln
```

```bash
cargo run --package node --bin client --features=client -- --grpc-endpoint 0.0.0.0:6000
grpcurl --import-path ./app/node/proto --proto an.proto -plaintext 0.0.0.0:6000 describe an.Node
grpcurl --import-path ./app/node/proto --proto an.proto -plaintext -d '{"msg": "I know how to fly planes"}' 0.0.0.0:6000 describe an.Node
```