[package]
name = "soroban_mock_dns"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
soroban-sdk = "22.0.0"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"